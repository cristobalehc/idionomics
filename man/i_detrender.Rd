% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/i_detrender.R
\name{i_detrender}
\alias{i_detrender}
\title{Linearly detrend idiographic time series by the time variable.}
\usage{
i_detrender(
  df,
  cols,
  idvar,
  timevar,
  min_n_subject = 20,
  minvar = 0.01,
  verbose = FALSE,
  append = TRUE
)
}
\arguments{
\item{df}{A dataframe. Any existing grouping is removed before processing.}

\item{cols}{A non-empty character vector of column names to detrend.}

\item{idvar}{A string naming the ID variable (one variable only).}

\item{timevar}{A string naming the time variable used for linear detrending. Must
be numeric and complete — no missing values allowed.}

\item{min_n_subject}{Integer. Subjects with fewer than \code{min_n_subject}
non-\code{NA} observations in a given column receive \code{NA} in the detrended
output. Mirrors the threshold used in \code{\link[=iarimax]{iarimax()}}. Defaults to 20.}

\item{minvar}{Numeric. Subjects whose pre-detrend or post-detrend variance
for a given column falls below \code{minvar} receive \code{NA} in the detrended
output. Mirrors the threshold used in \code{\link[=iarimax]{iarimax()}}. Defaults to 0.01.}

\item{verbose}{If \code{TRUE}, prints a description of the detrending rules.
Defaults to \code{FALSE}.}

\item{append}{If \code{TRUE} (default), returns the original dataframe with new
\verb{<col>_dt} columns appended. If \code{FALSE}, returns only the ID column, the
time variable, and the new \verb{_dt} columns.}
}
\value{
A dataframe with new \verb{<col>_dt} columns containing the
within-person linear-detrended residuals.
}
\description{
Linearly detrend idiographic time series by the time variable.
}
\details{
For each subject x column combination, the following rules are applied in
order:
\itemize{
\item All values \code{NA}: returns \code{NA}.
\item Fewer than \code{min_n_subject} non-\code{NA} values: returns \code{NA}.
\item Pre-detrend variance < \code{minvar}: returns \code{NA}.
\item Otherwise: fits \code{lm(col ~ timevar, na.action = na.exclude)} and takes the
residuals. If the post-detrend residual variance is also < \code{minvar},
returns \code{NA}.
}

The thresholds \code{min_n_subject} and \code{minvar} share the same defaults as
\code{\link[=iarimax]{iarimax()}}, but filtering here is applied \strong{per column independently}:
a subject can receive \code{NA} in one \verb{_dt} column while still producing valid
residuals in another. This differs from \code{\link[=iarimax]{iarimax()}}, which applies a joint
AND filter — a subject is excluded if \emph{any} series fails the thresholds.
The per-column design is intentional: it avoids unnecessary data loss and
lets you detrend variables in different combinations before deciding which
to pass to \code{\link[=iarimax]{iarimax()}}. The thresholds still serve the same protective
purpose — preventing data-poor or low-variance subjects from being detrended,
which would amplify numerical noise to apparent unit variance after
person-mean standardization and allow them to slip through \code{\link[=iarimax]{iarimax()}}'s
filters.
}
\examples{
local({
# Build a panel with a linear time trend embedded in x
set.seed(2)
panel <- do.call(rbind, lapply(1:3, function(id) {
  data.frame(
    id   = as.character(id),
    time = seq_len(25),
    x    = seq_len(25) + rnorm(25),  # linear trend + noise
    stringsAsFactors = FALSE
  )
}))

# Detrend x within each person (returns original df + x_dt)
result <- i_detrender(panel, cols = "x", idvar = "id", timevar = "time")
head(result)

# Return only id, time, and detrended columns
result_slim <- i_detrender(panel, cols = "x", idvar = "id",
                           timevar = "time", append = FALSE)
head(result_slim)
})
}
