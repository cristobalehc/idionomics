% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmstandardize.R
\name{pmstandardize}
\alias{pmstandardize}
\title{Person-level standardize (within-person z-score) time series.}
\usage{
pmstandardize(df, cols, id_var, verbose = FALSE, append = TRUE)
}
\arguments{
\item{df}{A dataframe. Any existing grouping is removed before processing.}

\item{cols}{A non-empty character vector of column names to standardize.}

\item{id_var}{A string naming the ID variable (one variable only).}

\item{verbose}{If \code{TRUE}, prints a description of the transformation rules
(default \code{FALSE}).}

\item{append}{If \code{TRUE} (default), returns the original dataframe with the
new standardized (\verb{_psd}) columns appended. If \code{FALSE}, returns only the ID column plus
the new \verb{_psd} columns.}
}
\value{
A dataframe with new \verb{<col>_psd} columns containing the
within-person z-scores.
}
\description{
Computes within-person z-scores for each variable in \code{cols}: for every
subject, subtracts that subject's mean and divides by that subject's SD.
}
\details{
For each subject, and each column:
\itemize{
\item All values \code{NA}: returns \code{NA}.
\item Fewer than 2 non-\code{NA} values: returns \code{0} (SD undefined; treated as
zero deviation).
\item Within-person SD near 0 (constant series): returns \code{0}.
\item Otherwise: returns \code{(x - mean(x)) / sd(x)}, computed with \code{na.rm = TRUE}.
}

Zero-variance subjects are retained as all-zero rows rather than dropped.
If you plan to pass output to \code{\link[=iarimax]{iarimax()}}, those subjects will be filtered
out by its \code{minvar} threshold.
}
\examples{
local({
# Build a small panel: 3 subjects, 10 observations each
set.seed(1)
panel <- do.call(rbind, lapply(1:3, function(id) {
  data.frame(
    id   = as.character(id),
    time = seq_len(10),
    x    = rnorm(10, mean = id * 2),  # different person-means
    y    = rnorm(10),
    stringsAsFactors = FALSE
  )
}))

# Standardize x and y within each person (append = TRUE, the default)
result <- pmstandardize(panel, cols = c("x", "y"), id_var = "id")
head(result)  # original columns + x_psd + y_psd

# Return only the ID and standardized columns
result_slim <- pmstandardize(panel, cols = "x", id_var = "id", append = FALSE)
head(result_slim)
})
}
\seealso{
\code{\link[=i_screener]{i_screener()}} for upstream data quality screening,
\code{\link[=i_detrender]{i_detrender()}} for the next pipeline step, \code{\link[=iarimax]{iarimax()}} for per-subject
modeling.
}
