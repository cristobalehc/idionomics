% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sden_test.R
\name{sden_test}
\alias{sden_test}
\title{Run Sign Divergence or Equisyncratic Null tests.}
\usage{
sden_test(
  iarimax_object,
  alpha_arimax = 0.05,
  alpha_binom = NULL,
  test = "auto",
  feature = NULL
)
}
\arguments{
\item{iarimax_object}{Your iarimax object.}

\item{alpha_arimax}{Critical value for per-subject ARIMAX coefficient significance. Defaults to 0.05.
In ENT mode this value serves double duty: it is both the threshold for counting a subject's
effect as significant (numerator) and the null probability for the binomial test (denominator).
Raising it simultaneously increases the count of significant effects and raises the null
expectation against which they are tested. Use \code{alpha_binom} to set the binomial null
probability independently if you need to decouple the two.}

\item{alpha_binom}{Null probability for the binomial test. Defaults to \code{alpha_arimax} for
ENT and \code{alpha_arimax / 2} for SDT. Supply this to override the default without changing
the per-subject significance threshold.}

\item{test}{Type of test, default to "auto". "auto", "SDT", and "ENT" are supported.
When \code{"auto"}, the selection between SDT and ENT is based on whether the pooled REMA
effect is statistically significant at a \strong{fixed threshold of 0.05}, regardless of
\code{alpha_arimax}. This is intentional: the pooled-effect pivot and the per-subject
significance criterion are treated as separate inferential decisions. Note that this
threshold also appears in the advisory messages printed under \code{"SDT"} and \code{"ENT"},
but does not affect any computation in those modes.}

\item{feature}{Feature name to run sden test. Defaults to iarimax focal predictor. Use your original name, function will automatically append "estimate_"}
}
\value{
An S3 object of class \code{sden_results} with two elements:
\code{sden_parameters} (a named list with test type, selection mechanism,
REMA beta, p-null, significance counts, and binomial p-value) and
\code{binomial_test} (the \code{htest} object from \code{stats::binom.test}).
Attributes \code{focal_predictor}, \code{id_var}, and \code{timevar} are
inherited from the input \code{iarimax_object}.
}
\description{
Run Sign Divergence or Equisyncratic Null tests.
}
\examples{
\donttest{
set.seed(1)
panel <- do.call(rbind, lapply(1:6, function(id) {
  x <- rnorm(30)
  data.frame(id = as.character(id), time = seq_len(30),
             x = x, y = 0.5 * x + rnorm(30),
             stringsAsFactors = FALSE)
}))

result <- iarimax(panel, y_series = "y", x_series = "x",
                  id_var = "id", timevar = "time",
                  min_n_subject = 20)

# Automatic test selection based on pooled REMA effect
sden <- sden_test(result)
summary(sden)

# Force ENT regardless of REMA
sden_ent <- sden_test(result, test = "ENT")
}
}
\seealso{
\code{\link[=iarimax]{iarimax()}} for per-subject modeling, \code{\link[=i_pval]{i_pval()}} for per-subject
p-values (must be applied before \code{sden_test()}),
\code{\link[=summary.sden_results]{summary.sden_results()}} for printing results.
}
